# Response to Reviewer Comments

| Problem | Current State | Solution | Ease of Implementation | Edited | Input file | Output file |
|---|---|---|---|---|---|---|
| **(1) Deposit-Type Heterogeneity.** REE deposits comprise multiple genetically distinct types (carbonatite, alkaline-peralkaline, ion-adsorption clay, placer, sedimentary phosphate, metamorphic/metasomatic), each governed by different ore-forming processes, source rocks, structural controls, and geochemical signatures. Current features are too coarse to distinguish these types, so cross-region generalization may be achievable but cross-type generalization has no geological basis. Evidence: F1 varies sharply across regions (0.742 North America vs 0.910 South America) — regions with mixed/different deposit types produce uneven results. Without stratifying by deposit type, the framework cannot produce a geologically meaningful prospectivity map. | `Dep_Type` has already been encoded into 9 groups (`is_group`: carbonatite, alkaline, mafic-ultramafic, felsic/pegmatitic, metamorphic/metasomatic, sedimentary, placer, supergene/lateritic, unclassified), but SHAP top-10 selection keeps only 2/9 groups (`_metasomatic`, `is_placer`) in the 13-dim node vector. F1 is currently only reported by region, not by deposit type, so it's unclear whether the disparity stems from geography or deposit-type composition. | **A. SHAP by deposit type** — split SHAP importance by each `is_group`, expecting carbonatite→pyrochlore/fluorite, placer→zircon/ilmenite, alkaline→nepheline/aegirine. Demonstrates the feature space is not too coarse.<br><br>**B. Entropy vs F1** — compute deposit-type entropy per region, compare against per-region F1, check correlation. Cheap, no retraining needed, directly explains the 0.742–0.910 disparity.<br><br>**C. Leave-One-Deposit-Type-Out (LODTO)** — run if A+B look good; analogous to Leave-One-Region-Out but held out by `is_group`: train on 8 groups, test on the remaining one, compare SPIRE's F1 vs a non-graph baseline.<br>　• F1 holds up and clearly beats the baseline → refutes the "not geologically defensible" claim.<br>　• F1 collapses → narrow the claim to "robust cross-region, limited cross-type," use A+B to explain the mechanism — turning the critique into a finding of the paper. | | **A (mixed).** SHAP-by-deposit-type (`experiment/run_deposit_type_analysis.py`, 3 variants) shows the model prefers a geographic shortcut over mineralogy when available: with Lat/Long+Region/Country present, `has_zircon`+geography dominate 8/9 groups (Lat+Long SHAP 3.4-6.3x the best mineral feature). Once all 3 geographic proxies are excluded, real group-specific mineral signal emerges (ratio drops to 1.7-3.5x; carbonatite→`has_barite`, felsic-pegmatite→`has_thorite`, supergene-lateritic→`has_gibbsite`, all geologically coherent). `is_placer` shows this coherent pattern regardless of geography. Follow-up F1 ablation (`run_ablation_coordinates.py`, not just SHAP) confirms a real, non-zero effect (smoke test: full F1=0.8909 vs no-coords F1=0.9009) — full sweep pending. Saved `result/deposit_type_shap.json` + 2 variant files, `result/ablation_coordinates.json` (pending).<br><br>**B (strong).** Deposit-type Shannon entropy per region vs SPIRE's per-region F1: Spearman r=-0.806 (p=0.005), Pearson r=-0.623 (p=0.054). North America/China (high entropy) score lowest F1; Oceania/South America (low entropy) score highest — directly explains the regional disparity the reviewer cites. Saved `result/deposit_type_entropy_vs_f1.json`.<br><br>**C — LODTO (code done, smoke-tested, full run pending).** `create_deposit_type_split_indices()` (`feature_engineering.py`) + new `run_lodto.py` (reuses `run_ablation.py`'s graph/train/predict via a new `split:` hook, no duplicated logic): SPIRE vs `ensemble_score` non-graph baseline, per group. Per-group test n: 116 (supergene-lateritic) to 1015 (placer) — no group is only "a few dozen" records, so no merging needed before the full run. 1-seed smoke test (mafic-ultramafic) inconclusive (SPIRE=0.8876 vs baseline=0.8916). Saved `result/lodto.json` (pending).<br><br>**Reporting guidance for the final writeup, once C's full sweep lands:**<br>　• A is mechanistic evidence only (shows *what* the model attends to), not proof of transfer — use to explain mechanism, not as primary validation.<br>　• B: report as *associated with*, not *causes* — only 10 regions, limited statistical power despite the significant p-value.<br>　• If C shows SPIRE beats the baseline with F1 not collapsing across most of the 9 groups → claim: "the model captures deposit-type-specific geological signal (once geographic shortcuts are excluded, per A) that also transfers to unseen deposit types (C), with regional disparity partly associated with deposit-type heterogeneity (B)."<br>　• If C collapses on most groups → not a paper failure — reframe as: "robust cross-region generalization, but transfer to genuinely unseen deposit types remains limited: cross-region generalization ≠ cross-type generalization," turning the reviewer's contested claim into a stated, defensible limitation.<br>　• Optional addition to A (not yet implemented): pairwise SHAP-profile similarity between deposit-type groups (cosine/Spearman across each group's full feature-importance vector) — distinct profiles strengthen "not a single generic pattern"; similar profiles is itself an informative finding either way. | `data/Global_REE_occurrence_database_processed.xlsx`<br>`result/gnn_inductive.json` | `result/deposit_type_shap.json`<br>`result/deposit_type_shap_no_latlong.json`<br>`result/deposit_type_shap_no_geo.json`<br>`result/deposit_type_entropy_vs_f1.json`<br>`result/ablation_coordinates.json` (pending full run)<br>`result/lodto.json` (pending full run) |
| **(2) Label & Feature Validity.** `has_ree` only measures whether `REE_Mins` mentions an REE mineral name, not whether the deposit is economically viable — a sample with a trace of monazite/xenotime is still labeled 1 even if it isn't an exploitable deposit. The negative class may simply reflect incomplete documentation, not a genuine absence of REE. The model may be learning documentation patterns more than genuine prospectivity, making the "prospectivity mapping" framing misleading. In addition, mineralogical/lithological features are all derived from free text, and part of the host lithology is rule-based inference rather than direct measurement — this uncertainty has never been quantified or propagated into the model. Accompanying suggestion: release the processed input data. | `has_ree` is defined exactly as above. Host lithology is inferred via rule-based Tier 1/Tier 2 tiers, with no confidence score carried into the feature. The raw dataset already has 2 **unused** fields: `Status` (Occurrence 1230 / Showing 799 / Deposit 671, with a `(?)` suffix marking uncertainty) and `P_Status` (Producer/Past producer/No production...) — exactly the kind of economic-significance information the reviewer is asking for, but the pipeline ignores it. Data Availability states "upon reasonable request," but `data/*.xlsx`, `gnn_features.parquet`, `gnn_target.npy`, `gnn_edge_*.npy` are actually already git-tracked in the repo — the statement is weaker than reality. | **A. Strict-label sensitivity** — 3 target levels from `Status`/`P_Status`: Level 1 = current `has_ree`; Level 2 = `Status ∈ {Deposit, Showing}` (decide whether to keep/drop the `(?)` suffix, report both ways); Level 3 = a "**confirmed deposit-oriented target**" (not called "economically significant" since `Status=Deposit` doesn't equate to economic viability) = `Status=Deposit` ∩ `P_Status ∈ {Producer, Past producer, Byproduct producer}`. Re-run evaluation at each level, check whether F1 declines gradually or collapses abruptly:<br>　• *Gradual decline* (e.g. 0.90→0.87→0.84): predictive signal persists as the target tightens, the model isn't merely "riding" the loose label definition — the generalization claim holds, just state how much performance drops as the target tightens.<br>　• *Abrupt collapse* (e.g. 0.90→0.55 or lower at Level 2/3): the model is mainly matching `has_ree` (the loose definition) and nearly loses predictive power once the target is tightened to true economic significance — the claim must be narrowed, and it can't be called "prospectivity mapping" until retrained/redesigned around a stricter target.<br>　→ *Resolves*: directly answers whether the label measures "prospectivity" or merely whether an REE mineral is mentioned in the record.<br><br>**B. Mineral/inferred-feature ablation** — 3 spots need to be patched before running it, otherwise it will leak backward:<br>　• *Mineral Feature Engineering*: drop the 6 mineral-derived columns (`has_ilmenite`, `has_magnetite`, `has_barite`, `has_zircon`, `has_pyrochlore`, `has_fluorite`) from the node vector for Model B.<br>　• *Feature Selection/Ensemble prior*: retrain XGBoost/CatBoost/RF without the mineral features and recompute `ensemble_score` — if only the node-vector columns are dropped while keeping the old ensemble, `ensemble_score` still carries mineral signal since it was trained on the original feature set.<br>　• *Graph Construction*: rebuild all edges (`w_feature` cosine similarity) from the mineral-stripped feature vector, since `w_mean`/`w_max` depend on the original graph — without rebuilding, they still leak.<br>　• *Dimension compensation*: Model B would be left with only ~5 clean dimensions, biasing F1 downward purely from lack of information — compensate with the 9 `is_group` indicators (Comment 1) for a fair comparison.<br>　• *Interpretation*: a sharp F1 drop here doesn't automatically mean "learning documentation patterns" — diagnostic minerals are genuine geological signal. This ablation only answers "how much do mineral features contribute."<br><br>**C. Documentation-completeness / artifact probe**<br>　• C.a — split the negative class into well-documented vs poorly-documented (number of non-empty fields, length of `Comments`/`Loc_Note`, presence of `Ref_List`), evaluate F1 separately for each group.<br>　• C.b (sharper) — a baseline classifier using only non-geological completeness/meta-proxy features to predict `has_ree`. AUC ~0.5 → refutes the "predicting documentation patterns" claim; a clearly high AUC → confirms the label rides on documentation level, disclose it directly in Limitations.<br>　→ *Resolves*: answers "the negative class may reflect incomplete documentation rather than a genuine absence of REE" — verified with data instead of left open.<br><br>**D. Lithology-inference ablation** — tag each record with a confidence tier (Direct/Tier1/Tier2/Unclassified) right at the inference step, report F1 by tier; compare the full model vs. one without inferred-lithology features to quantify circularity risk.<br>　→ *Resolves*: quantifies the uncertainty of rule-inferred features (host lithology) that the reviewer points out as never having been measured/propagated into the model.<br><br>**E. Reframing the text** — change "prospectivity mapping" → "REE mineral-occurrence indicator classification from geological records"; add a paragraph to Limitations on negative-class ambiguity (negative = "not documented," not "confirmed absent").<br>　→ *Resolves*: reduces the overclaim the reviewer calls "somewhat misleading" in how the paper describes its own task. | | **A downgraded to optional — same reasoning already applied to (8) and (18)-C.** (2)-A's original purpose (test whether `has_ree` holds up under a stricter, economic-significance-adjacent target, via `Status`/`P_Status`) was to defend the "prospectivity mapping" claim against the label being too loose for it. Now that (10) is decided as **Recast**, the paper no longer claims prospectivity mapping or implies `has_ree` approximates economic significance — it states the task is exactly what `has_ree` measures (whether an REE mineral is mentioned in the record). The specific overclaim (2)-A was built to test for no longer exists in the manuscript, so there's nothing left for A to defend or narrow. **B/C/D remain fully in scope, unaffected by this** — none of them depend on the "prospectivity mapping" framing: B and D test whether the model rides on documentation artifacts vs. genuine mineral/lithology signal (a modeling-validity question, independent of what the task is called), and D is now more load-bearing since it connects directly to (11)'s leakage fix. **E partially done.** The wording-replacement half ("prospectivity mapping" → classification framing) is covered by (10)'s Recast — same change, no need to repeat it. But (2)-E's other half — "add a paragraph to Limitations on negative-class ambiguity (negative = 'not documented,' not 'confirmed absent')" — is a distinct addition (10)'s Edited note does **not** mention and has **not** been written yet; still pending. **Exact edit drafted (not yet inserted into `main.md`, same reason as (10)/(18)-A — not tracked in this repo):**<br>　• *Location*: `\subsection{Limitations and Future Work}` (LaTeX), right after the "Firstly... Secondly... Thirdly... Finally..." paragraph's closing sentence — the one ending "...may necessitate re-tuning of $\sigma$, $k$ and the distance cap." — and before the "Future work should study hybrid architectures..." paragraph.<br>　• *New paragraph text* (verbatim, may be lightly re-worded to match the surrounding LaTeX style): "A further limitation concerns the definition and provenance of the modeling target and several input features. The binary target `has_ree` records whether an REE mineral is mentioned in the `REE_Mins` field of a given database record; it is defined at the level of documented mineral occurrence rather than confirmed economic viability and should not be equated with a resource or deposit-grade indicator. Correspondingly, the negative class (`has_ree` = 0) denotes records without a documented REE mineral rather than confirmed geological absence, since the underlying database may simply not have recorded such information for a given site. Several mineralogical and lithological input features are likewise derived from free-text database fields, and host lithology in particular was reconstructed for a subset of records through rule-based inference from associated deposit-type and mineral descriptors rather than direct observation, which introduces feature-level uncertainty that is not currently quantified or propagated into the model."<br>　• *Dependency note*: this paragraph doesn't depend on whether (10)'s "prospectivity"→"classification" wording swap has landed — it stands on its own either way — but for overall consistency it's best inserted at the same time as (10)'s reframing pass, since both live in the same Limitations paragraph block. **B implemented (code done, smoke-tested, full sweep deferred).** New script `experiment/run_ablation_minerals.py` + new `exclude_minerals` flag on `run_ablation.py`'s `_build_graph_artifacts`/`run_ablation_sweep`: drops the 6 named mineral columns from the SHAP *candidate* pool (not just the final node vector) before top-10 selection, so all 3 leak-back paths the solution calls out are patched by construction — the ensemble (fit on the same stripped `X`) can't smuggle mineral signal back through `ensemble_score`, and the graph's cosine-similarity term (built from the now mineral-free `X_selected`) can't leak it through `w_mean`/`w_max` either, without needing separate patches for each. Dimension compensation: the 9 `is_group` indicators (Comment 1) are appended to `X_selected`, skipping any already SHAP-selected (verified no duplicates). **Verified**: `py_compile` + import; unit-checked the dimension logic directly (Middle East) — `with_minerals` node vector is 11-dim pre-augmentation (10 SHAP + ensemble) as before, `no_minerals` comes out at 18-dim (SHAP selected 2 `is_group` columns naturally from the reduced pool, so only 7 more were appended, not all 9 — confirms the skip-duplicates logic works); real smoke run (Middle East, 1 seed) gives a real, non-zero gap — with_minerals F1=0.8909 vs no_minerals F1=0.8785. **Directly re-verified all 3 leak-back paths the solution calls out** (not just inferred from reading the code path): built the `no_minerals` artifacts for Middle East and inspected each object directly — (1) node vector: none of the 6 columns in `X_selected.columns`; (2) ensemble: read each fitted model's own feature-name list (`feature_names_in_` for XGBoost/RandomForest, `feature_names_` for CatBoost) — all 3 report 289 features, none of the 6 minerals present; (3) graph: none of the 6 columns in `X_geo.columns` (the cosine-similarity input). Caught and fixed one bug in this verification itself while doing it: the first pass used a fallback (`get_booster().feature_names`) for retrieving CatBoost's feature list, which doesn't exist on `CatBoostClassifier` — it silently returned `None`, making that specific check vacuously pass without actually checking anything; switched to CatBoost's real `feature_names_` attribute and reran, confirmed genuinely clean. Full sweep (10 regions × 5 seeds) deferred to the end-of-Group-3 batch run. **D implemented (code done, verified, tier stats run for real, F1 comparison smoke-tested, full sweep deferred).** New `classify_host_lith_confidence()` in `preprocessing/host_lith.py`: determined purely from raw inputs + `infer_host_lith`'s own return value (checks for its literal `"(inferred, low confidence)"` marker, and whether raw `Host_Lith` was null) — does not duplicate or alter `infer_host_lith`'s branching logic at all, so `Host_Lith`/`is_group` values are unaffected (the exact risk flagged earlier when discussing LODTO's dependency on `is_group`). New script `experiment/run_lithology_ablation.py`, 2 parts: **Part 1 (tier tagging) — run for real.** Direct=1978, Tier1=908, Unclassified=173, Tier2=53 (processed dataset, 3112 records). **Cross-checked against Comment 11's fix log**: Tier1+Tier2+Unclassified = 908+53+173 = 1134, exactly matching the "Host_Lith filled for 1134 records" already reported there — strong internal consistency confirmation, not just a plausible-looking number. Saved `result/lithology_confidence_tiers.json` (per-record tier — reusable by Comment 7-A once Exp1 per-node predictions exist, same reuse pattern as (2)-C.a's completeness bucket). **F1-by-tier split deferred** (same per-node-prediction gap as (2)-C.a/Comment 7/(18)-B). **Part 2 (with/without the 9 `is_group` indicators) — code done, smoke-tested.** Reuses `run_ablation.py`'s new `exclude_lithology` flag (same pattern as `exclude_minerals`/`exclude_coords`), no dimension compensation (not called for in the solution text, unlike (2)-B). **Verified**: `py_compile` + import; directly re-checked all 3 leak-back points the same way as (2)-B (not just inferred) — `X_selected` (10-dim, exactly as expected with no compensation), all 3 ensemble models' own `feature_names_`/`feature_names_in_` (286 features each), and `X_geo` all confirmed free of the 9 `is_group` columns; real smoke run (Middle East, 1 seed) — with_lithology F1=0.8909 vs no_lithology F1=0.8704, a real, non-zero gap. Full sweep (10 regions × 5 seeds) deferred to the end-of-Group-3 batch run. **C.b done — finding: meaningfully above chance, partially supports the reviewer's concern.** New script `experiment/run_documentation_completeness.py`: builds completeness features from the RAW database (`Comments`/`Loc_Note`/`Ref_List`/`Rec_Note`/`Dep_Note`/`Stat_Note`/`Expl_Note`/`P_Note`, joined via `ID_No`, same de-dupe as the one duplicated ID used elsewhere) — `n_nonempty_fields` (count of non-null among the 8), plus character length of `Comments`/`Loc_Note`/`Ref_List`. Found `Ref_List` is populated for 100% of records (0% null), so its *presence* has no variance — used its length instead as the completeness proxy, more informative than the binary presence check the solution originally proposed. A RandomForest trained on ONLY these 4 completeness features (no geological/mineral content at all) to predict `has_ree`, 5-fold CV: **AUC = 0.753 ± 0.014** — clearly above the ~0.5 that would refute the concern, though below a "near-perfect" level. **This partially supports the reviewer's concern**: documentation completeness alone carries real, non-trivial predictive signal for the current label, independent of any actual geological content — worth disclosing in Limitations per the original C.b instruction. **Specificity check (follow-up, not in the original solution text) — finding: NOT an REE-specific artifact, a general property of the compiled database.** New function `run_specificity_check()` in the same script: reran the identical completeness-only probe against 5 non-REE-diagnostic `has_*` mineral columns (excludes zircon/ilmenite/pyrochlore/fluorite/magnetite, already implicated in the REE label itself). Result: `has_rutile` AUC=0.716, `has_apatite` AUC=0.722, `has_pyrite` AUC=0.701, `has_leucoxene` AUC=0.826, `has_cassiterite` AUC=0.707 — all in the same 0.70-0.83 range as `has_ree`'s 0.753, one even higher. **This substantially reframes the finding**: documentation completeness predicts the mention of essentially any mineral about equally well, not specifically REE — supports describing this in the paper as an inherent property of the USGS-derived compilation (sparse records report fewer minerals of every kind, not just REE), rather than an REE-task-specific artifact or a flaw unique to SPIRE's label choice. Also confirmed via a direct (non-probe) check: records with `has_ree=0` have a median of 0 minerals reported at all across the 179 `has_*` columns the model actually uses as features (mean 1.08), vs a median of 4 (mean 4.13) for `has_ree=1` (point-biserial r=0.436, p<1e-144) — meaning this signal is not confined to the raw-text fields the model never sees (`Comments`/`Ref_List`), it is also indirectly encoded in the sparsity pattern of the mineral features the model *does* use, which is the more direct pipeline-relevant version of the concern. A region-level confound check (does this just track a few well-documented regions?) found the opposite of what a confound would need: region-level completeness vs. region-level `has_ree` rate correlates at **r=-0.271** (weak negative) — the record-level relationship holds *within* regions, not because of *between*-region differences, ruling out region as the explanation. Saved `result/documentation_completeness.json` (now includes `c_b_specificity_check`). **Reviewer-response arguments (for the revision letter, not yet written into the manuscript):**<br>　1. *Not REE-specific.* "Documentation completeness correlates with the reporting of any individual mineral in this compilation (AUC 0.70-0.83 across 5 unrelated minerals — rutile, apatite, pyrite, leucoxene, cassiterite), not specifically REE (AUC 0.753 sits squarely inside that same range) — this reflects a general property of the source database rather than an REE-specific artifact of the target label." Strongest argument, directly evidenced.<br>　2. *Dataset-level issue, applies equally to every model compared.* `has_ree` is defined purely from free-text field presence (`REE_Mins.notnull()`) — a property of the USGS-derived compilation, not a SPIRE-specific design choice. Every baseline (XGBoost, MLP, ...) and SPIRE are trained/evaluated against the exact same label, so this limitation does not skew the *relative* comparison between models — it only bounds how the *absolute* numbers should be interpreted ("does the model learn real REE signal" vs. "does it partly track documentation density"), which belongs in Limitations, not as a reason to distrust the model comparison itself. **C.a groundwork done, full split deferred.** Built the well-documented/poorly-documented bucket (median split on `n_nonempty_fields`: 1113 well-documented vs 1999 poorly-documented) and saved per-record scores to `result/documentation_completeness_scores.json` — reusable as-is by Comment 7-A (same completeness signal, avoids duplicating this logic). The actual "F1 split by bucket" needs per-node TP/FP/FN/TN from Exp1, not yet saved (same gap noted for Comment 7 and (18)-B) — deferred until Exp1 is instrumented (planned jointly with Comment 7-A). **Verified**: `py_compile`; real run on the full dataset (3112 records) completes cleanly, printed distributions are sane (e.g. median 5/8 non-empty fields, `Comments` present in only 35.6% of records matching the earlier-established 64.4% null rate). | `data/Global_REE_occurrence_database.xlsx` (raw)<br>`data/Global_REE_occurrence_database_processed.xlsx`<br>`experiment/run_ablation.py`<br>`preprocessing/host_lith.py` | `result/documentation_completeness.json`<br>`result/documentation_completeness_scores.json`<br>`result/ablation_minerals.json` (pending full run)<br>`result/lithology_confidence_tiers.json`<br>`result/ablation_lithology.json` (pending full run) |
| **(3) Related Work Coverage.** Section 2 is too thin, citing only 1 paper from the last 2 years, and doesn't cover the fast-growing GNN-MPM literature. Not engaging with recent research directions means the paper misses the chance to position SPIRE against the state of the art. | The "Graph neural networks in the geosciences" paragraph has only 4 generic refs, not broken down by sub-area, with no comparison of SPIRE against each direction. | Rewrite the paragraph around the 5 sub-areas the reviewer names, each with 1-2 sentences of literature (real references need to be looked up, not invented) plus 1 sentence positioning SPIRE:<br>　• **Heterogeneous graph construction** — SPIRE uses a single-relation graph (spatial + feature similarity merged into one edge type); frame this as a deliberate simplification.<br>　• **Multi-scale graph learning** — SPIRE uses a single-scale $k$-NN; contrast with multi-scale approaches.<br>　• **Self-supervised/contrastive learning on graphs** — SPIRE is supervised end-to-end; the reason is the dataset is only ~3.1k nodes, too small for pretraining at scale.<br>　• **Graph-based uncertainty quantification** — SPIRE currently lacks this; link to D (lithology confidence tier) proposed in Comment (2).<br>　• **Interpretable GNN by deposit type** — SPIRE uses post-hoc SHAP on the tabular ensemble, not interpretable-by-design; acknowledge the difference. | ✓ | | text only | text only |
| **(4) Tiny Test Set & Statistical Uncertainty.** Exp 2/3/5/6 hold out only 5 test nodes/region — one flipped case can materially change F1, ROC-AUC/PR-AUC ~1.000 is an artifact of sample size, not genuine performance; Limitations acknowledges this but Fig 4b/4c, 7b/7c still present it prominently. Pure random splitting may also place train/test observations near each other spatially — a weak test of extrapolation. | Results text already admits ROC-AUC/PR-AUC ~1.000 isn't informative, but figures still show it prominently (text/figure mismatch). Random split is purely random, no spatial blocking — exactly the risk the paper itself cites in Related Work (Roberts2017) but never applies internally. No class counts per fold, no paired significance test, no explicit priority for Exp1 in cross-region claims. | **A.** Increase holdout to 10-20%/region instead of fixed 5 nodes, redesign Exp 2/3/5/6.<br><br>**B.** Add spatial block/buffered CV — exclude train nodes too close to test nodes (reuse the buffer-exclusion idea from (10), radius from mean $k$-NN distance in (7)-B).<br><br>**C.** Report class counts per fold.<br><br>**D.** Pool predictions across runs on the new (larger) test sets, recompute ROC-AUC/PR-AUC with a 95% CI instead of a single point estimate.<br><br>**E.** Paired comparison (t-test/Wilcoxon) between SPIRE and the strongest baseline (MLP) across runs.<br><br>**F.** Remove/de-emphasize ROC-AUC/PR-AUC in Fig 4b/4c, 7b/7c, keep F1 prominent; state that cross-region transfer claims rely mainly on Exp1 (leave-one-region-out). | ✓ (after 12, 13, 14 — see §2) | **A+B implemented and verified with real smoke runs on all 4 affected experiments (Exp1/Exp4 untouched — leave-one-region-out, not a fixed-n holdout).** Redesigned the split for Exp2 (`run_gnn_per_region.py` + `run_baseline_per_region.py`), Exp3 (`run_inductive_sample.py`, Exp6 inherits via import), Exp5 (`run_exp5_proportion_per_region.py`): test holdout is now `_TEST_RATIO=0.15` (15%, midpoint of the reviewer's 10-20% range) of each region's nodes instead of a fixed n=5, and train/val candidates within a spatial buffer of any test node are excluded entirely (Comment 4-B) — buffer radius defaults to the region's own mean k-NN distance (`compute_mean_knn_distance_km`, reusing the same k the region's own graph uses), not an arbitrary fixed number. Both new utilities (`compute_mean_knn_distance_km`, `apply_spatial_buffer`) added to `preprocessing/feature_engineering.py`, unit-tested in isolation first (synthetic 2-cluster data: tiny buffer keeps same-cluster points, huge buffer excludes everything, cross-cluster points always pass — all confirmed before touching real experiment code). **Two shared split functions** (also added to `feature_engineering.py`, not duplicated per-script): `create_buffered_region_split_indices` (Exp2/Exp5 — train/val confined to the same region) and `create_buffered_sample_split_indices` (Exp3/Exp6 — train/val spans the whole dataset). This was a deliberate refactor, not just a redesign: `run_baseline_per_region.py`'s own docstring requires it produce "the same 5 test nodes (same seed)" as `run_gnn_per_region.py` — duplicating the new split logic into 2 files would have risked them silently drifting apart under future edits, so both now call one shared function with matching `k_for_buffer` (confirmed identical split output directly, not just by inspection). Same reasoning applied to Exp6, which already imported Exp3's split function rather than having its own copy. Added a safety fallback: if buffering would leave too few train/val candidates (small/dense regions), buffering is skipped for that region with `split_info["buffer_fallback"]=True` rather than crashing or training on 1-2 nodes. **Verified with real end-to-end runs** (Middle East, the smallest/most fragile region, n=63) on all 4 scripts: `run_gnn_per_region.py` and `run_baseline_per_region.py` produced byte-identical splits (n_test=9, buffer_km=254.1, 21 candidates excluded, n_train=27, n_val=6) as required; `run_inductive_sample.py` and `run_exp6_proportion_inductive_sample.py` also produced matching splits (n_test=9, buffer_km=371.2, 31 excluded) for the same region/seed. All 4 scripts trained and predicted without error, sane non-degenerate metrics. **D implemented (instrumentation + analysis script done, verified end-to-end with real 2-seed data, full pooling deferred).** Instrumented `run_gnn_per_region.py`, `run_baseline_per_region.py`, `run_inductive_sample.py` to save per-node `y_true`/`y_prob`/`y_pred` (`run_exp5_proportion_per_region.py`/`run_exp6_proportion_inductive_sample.py` already had this from Comment 18-B, no change needed there). Same aggregation-breaking risk as (7)-A's `run_gnn.py` fix applies here too — kept predictions in a **separate** `"predictions"` key everywhere, never merged into the dict that `_avg_metrics`/`_avg_model_metrics` sums across regions; `run_inductive_sample.py`'s `_run_baseline_for_region`/`_run_gnn_for_region`/`_predict_with_artifacts` needed their return signatures changed to `(metrics, predictions)` tuples to keep this separation (single call site each, checked before changing). New script `experiment/run_pooled_auc_ci.py`: pools (y_true, y_prob) across every (seed, region) for a given model across all 5 result files (3 different pooling functions for the 3 different schemas — Exp2's flat `per_region → predictions`, Exp3's nested `{spire,gcn,gat}.per_region` + `baselines.predictions_per_region`, Exp5/6's existing (18)-B schema), computes the point estimate plus a node-level bootstrap 95% CI (1000 resamples) for ROC-AUC and PR-AUC — noted as a simplification (doesn't model the region/seed clustering) in the docstring, consistent with how such CIs are typically reported. **Verified**: `py_compile`; unit tests confirm the bootstrap CI is correct at 3 checkpoints (near-perfectly-separated synthetic data → CI tight near 1.0; single-class `y_true` → correctly returns `None`, AUC is undefined there; near-chance random data → CI straddles 0.5) and that all 3 pooling functions correctly concatenate synthetic multi-seed payloads; ran against the *current* (pre-instrumentation) result files to confirm graceful degradation (all 5 correctly report "no usable pairs", no crash). **Real end-to-end run**: generated one real per-region result (Middle East, seed=42) via `run_gnn_per_region.py`'s actual `_run_region` — found all 9 sampled test nodes were `has_ree=1` (single-class), so ROC-AUC was correctly `None` for that one fold alone; this is itself a concrete illustration of the reviewer's exact concern (small per-fold samples can still be single-class even at 15% holdout) and directly motivates pooling. Added a 2nd real seed (seed=7) for the same region and re-pooled: n=18 pooled nodes (15 positive / 3 negative), SPIRE ROC-AUC = 0.578 [0.235, 0.941], GCN = 0.756 [0.471, 1.000], GAT = 0.622 [0.292, 1.000] — genuine, non-degenerate point estimates with honestly wide CIs (as expected from only 2 pooled seeds), a much more defensible reporting format than a single fold's misleadingly precise 1.000. **Full pooling across all regions × 5 seeds** deferred to the end-of-Group-3 batch run, same as everything else. **C implemented and run for real (full numbers, all 10 regions × the exact 5 seeds each experiment's README table uses — no model training needed, so no smoke-test/full-run distinction here).** New script `experiment/run_class_counts_per_fold.py`: for each of Exp2/3/5/6, replicates the *exact* split each real script will produce (same `test_ratio`/`k_for_buffer`/seed, via the same shared split functions; for Exp3/6 specifically mirrors the stateful `rng` shared across the region loop within a seed, not a fresh RNG per region) using the README-documented seed lists (Exp2=[42,50,32,17,21], Exp3=[42,23,10,16,49], Exp5=[42,18,20,48,50], Exp6=[42,20,11,60,48]), and reports has_ree=0/1 counts per fold (50 folds = 10 regions × 5 seeds, per experiment). **Finding**: Middle East (n=63, the smallest region) has 2-3 of its 5 seeds landing on a fold with ≤1 minority-class test node in every one of the 4 experiments (e.g. Exp2 seed=42: 9 positive/0 negative; Exp5 seeds 42/18/20: all 9/0) — even at the new 15% holdout, Middle East's high overall positive rate (~87%) still produces single-class or near-single-class folds under several seeds. This is real, concrete evidence (not hypothetical) for the reviewer's "one flipped case can materially change F1" concern surviving the (4)-A/B redesign for this specific region, and is exactly why (4)-D's cross-seed pooling matters. Saved `result/class_counts_per_fold.json`. **E implemented (code done, verified end-to-end on real — if pre-(4)-A/B, soon-to-be-superseded — result files, no new instrumentation needed).** New script `experiment/run_paired_significance_test.py`: paired t-test + Wilcoxon signed-rank test between SPIRE and MLP across the 5 seeds, using each experiment's own headline metric (F1 for Exp2/Exp3, MAE for Exp5/Exp6 — a proportion-prediction task, lower is better) and only the per-seed `avg_metrics`/`aggregated_metrics` already saved (no per-node data needed, unlike D). Exp2's SPIRE/MLP values live in 2 separate result files (`gnn_per_region.json`/`baseline_per_region.json`), paired by matching `seed`; Exp3/5/6 keep both in the same file already. **Bug caught and fixed during unit testing**: the initial degenerate-case guard (`not np.allclose(diffs, diffs[0])`) incorrectly treated a *constant* non-zero paired difference (SPIRE beating MLP by exactly the same margin on every seed — the single strongest possible paired effect) as "not meaningful" and skipped the test entirely; verified directly that `scipy.stats.ttest_rel` actually handles this case correctly on its own (returns a huge t-statistic and tiny p-value, just a harmless floating-point precision warning) — fixed by only short-circuiting the genuinely degenerate case (SPIRE exactly equals MLP on every seed, zero difference), letting scipy handle everything else natively. **Verified**: `py_compile`; unit tests confirm all 4 cases now behave correctly (consistent difference → significant, as it should be; noisy/no difference → not significant; SPIRE≡MLP → correctly `None` with a clear note; single pair → correctly `None`, insufficient data). **Real end-to-end run** (against the current, pre-(4)-A/B result files — numbers will change after the real rerun, but the code path is fully exercised): all 4 experiments produced n=5 pairs with sane statistics, none reaching p<0.05 (Exp2 F1: SPIRE 0.875 vs MLP 0.867, p=0.39; Exp3 F1: 0.895 vs 0.928, p=0.11; Exp5 MAE: 0.184 vs 0.135, p=0.17; Exp6 MAE: 0.133 vs 0.126, p=0.56) — consistent with the broader finding already established this session that SPIRE no longer leads after the Group 1 leakage fixes. Saved `result/paired_significance_test.json`. **F not yet done** — pure text/figure editing, no code. Full sweep (all regions × 5 seeds, all 4 redesigned scripts) deferred to the end-of-Group-3 batch run — same as every other pending item; C's numbers above are already final (don't depend on the rerun), D and E will be recomputed once the rerun lands. | `preprocessing/feature_engineering.py`<br>`experiment/run_gnn_per_region.py`<br>`experiment/run_baseline_per_region.py`<br>`experiment/run_inductive_sample.py`<br>`experiment/run_exp5_proportion_per_region.py`<br>`data/Global_REE_occurrence_database_processed.xlsx`<br>`result/gnn_per_region.json`, `result/baseline_per_region.json`, `result/inductive_sample.json`, `result/exp5_proportion_per_region.json`, `result/exp6_proportion_inductive_sample.json` (E's current-file smoke run) | `result/gnn_per_region.json`, `result/baseline_per_region.json`, `result/inductive_sample.json`, `result/exp5_proportion_per_region.json`, `result/exp6_proportion_inductive_sample.json` (all now include `split_info`/`test_ratio_per_region`/per-node `predictions`, pending full rerun)<br>`result/pooled_auc_ci.json` (pending full pooling)<br>`result/class_counts_per_fold.json` (final, real numbers)<br>`result/paired_significance_test.json` (pending rerun for final numbers) |
| **(5) Undefined Region Boundaries.** The 10 regions have never been specified in terms of boundaries, rationale, or whether graph edges cross regional boundaries. | `Region` already exists in the raw USGS data, never explained in the text. `fig:world_overview` already exists but doesn't show boundaries. The graph uses spatial $k$-NN, unconstrained by region. | **A.** Add a supplementary table mapping Region → list of countries + $n$ records, taken directly from the original `Region` field. Answers both "boundaries" and "rationale" (rationale = inherited from USGS's own classification, not invented).<br><br>**B.** Extend the existing `fig:world_overview`: add a country outline/legend by region instead of just points.<br><br>**C.** Add 1-2 sentences to Graph Construction or Training and Evaluation Strategy stating clearly that edges rely entirely on spatial $k$-NN, unconstrained by region, so they cross regional boundaries in the training graph; the held-out region in Exp 1 is entirely excluded from the training graph. | ✓ | **A implemented.** Inserted into `## Dataset, Data Cleaning and Preprocessing` (`#data`), right after "Records from Antarctica were excluded due to limited data availability." and before "Of the 3,112 records retained...": a new sentence stating the 10 regions are inherited directly from the `Region` field of the USGS compilation (not a boundary defined for this study), predominantly but not strictly aligned with national borders — a small number of China/Canada/Russian Federation records are assigned to a neighboring region on record-level geological/administrative grounds; plus a new supplementary table listing all 10 regions → constituent countries + record count $n$ (Africa 235, China 214, East Asia 358, Europe 383, Middle East 63, North America 423, Oceania 504, Russian Federation 147, South America 253, South and Central Asia 532), taken directly from the existing `Region` field. **C implemented.** Inserted into `## Graph Construction` (`#sec:Graph_Construction`), right after "...the single closest neighbor of each node was always retained to prevent isolated nodes." and before "Edge weights $w_{ji}$ were designed to capture...": two new sentences stating that $k$-NN edge selection is purely geographic and unconstrained by region, so edges routinely cross regional boundaries (e.g. Europe↔Middle East, Russian Federation↔East Asia), and that in the leave-one-region-out setting (Exp 1) the held-out region's nodes are entirely excluded from the training graph, so cross-region connectivity only applies among the 9 training regions. **B implemented.** Added `build_country_to_region()` and `add_region_country_shading()` to `visualize_case_study.py`: country→region mapping is computed directly from the existing `Region`/`Country` columns (majority vote per country, since a handful of records for China/Canada/Russian Federation fall under a neighboring region — e.g. Canada: 251 North America vs 3 Europe), then each country polygon (Natural Earth 50m `admin_0_countries`, matched by `NAME`/`NAME_LONG`) is filled with its region's color (`REGION_COLORS`, alpha=0.30, zorder between LAND and COASTLINE/BORDERS so the existing thin country-border lines still draw on top) — giving a shaded country outline by region instead of just scatter points, reusing the same `REGION_COLORS` so the existing "Region" legend now covers both the shading and the points. **Verified**: `py_compile`; real run of `visualize_case_study.py` — 105/106 countries in the dataset matched to a region (only French Guiana, 1 record, has no separate shape at 50m resolution and is left unshaded — its scatter point still shows); regenerated `figures/fig_world_overview_exp1.png` and visually confirmed each region's countries are tinted correctly, borders/coastline/points/legend/annotations all still render as before. **Caption addendum (for the paper, not yet inserted since `main.md` isn't tracked in this repo):** add one sentence to the `fig:world_overview` caption — "Countries left unshaded have no occurrence records in the compiled dataset and are therefore not assigned to any of the ten regions." — clarifying that the 137 blank countries (verified via code: NE 50m country list minus the countries present in `Country`) reflect absence of data, not a mapping gap. | `data/Global_REE_occurrence_database_processed.xlsx`<br>`visualize_case_study.py` | `figures/fig_world_overview_exp1.png` (B)<br>A/C = manuscript text insertion (external `main.md`, not tracked in this repo) |
| **(6) Ablation "Proves" Graph Contributes Little.** Removing the ensemble prior causes a sharp F1 drop (0.939→0.820, Exp3a), while removing edge-stat only causes a small drop (0.844→0.828, Exp1) → concluding graph structure contributes very little, the ensemble prior is the real driver, undermining the central claim about relational learning; SPIRE is effectively "an ensemble model with graph-based fine-tuning." | The ablation is nested (13-dim → drop edge-stat → 11-dim → also drop ensemble → 10-dim), but the Discussion compares 13-dim directly to 10-dim and calls it the "ensemble prior effect" — the 10-dim condition has actually lost **both** edge-stat and ensemble. The raw data (`ablation_feature_exp3.json`) already has the missed intermediate condition: no_edge_stats (keeping ensemble) at Exp3a gives F1=0.900, so edge-stat alone contributes 0.039, not "very little." More importantly: all 3 conditions **still use message passing** — the ablation only strips 2 scalar features from the node vector, never turns off the graph itself. | **A.** Add a new **12-dim condition (drop ensemble, keep edge-stat)**, run in parallel with the existing 11-dim (drop edge-stat, keep ensemble) — both compared directly against the full 13-dim.<br>　→ *Resolves*: gives the correct pair of numbers needed to fairly compare the isolated effect of ensemble vs. edge-stat, instead of numbers confounded by the nested design.<br><br>**B.** State clearly in the text: the ablation only removes input features, graph/message-passing (GraphSAGE aggregation) still operates under every condition — so this ablation cannot by itself prove "graph structure contributes little."<br>　→ *Resolves*: preempts the misreading (category error) the reviewer is making.<br><br>**C (optional, stronger but more effort).** Compare SPIRE (with message passing) against the same node vector fed through a plain MLP (no graph) — isolates the true contribution of message passing, independent of the ensemble/edge-stat features. Run (A+B) and C; whichever shows the larger gap gets used as the stronger piece of evidence. | ✓ (after 12, 13, 14 — see §2) | **A implemented and verified (smoke-tested, full sweep deferred).** Added a 4th mode `no_ensemble_keep_edge_stats` (12-dim: drops `ensemble_score`, keeps `w_mean`/`w_max`) to `run_ablation.py`'s existing mode framework — generalized the 2 hardcoded gates (`mode=="no_ensemble"` for excluding the ensemble score, `mode=="full"` for including edge-stats) into 2 reusable sets (`_MODES_WITHOUT_ENSEMBLE`, `_MODES_WITH_EDGE_STATS`) so the new mode composes cleanly with the existing 3, at both train-graph-build and test-time-inductive-prediction call sites. **Verified**: `py_compile`; real run (10 regions, 1 seed) — full (13-dim) F1=0.8328 vs no_ensemble_keep_edge_stats (12-dim) F1=0.8046, delta -0.0283 — this is the correct isolated ensemble-prior contribution the nested 13→11→10 design couldn't give directly. Full 5-seed sweep deferred to the end-of-Group-3 batch run. **B — text only, not yet written into the manuscript.** **C implemented and verified (smoke-tested, full sweep deferred) — merged with (17)-B, same experiment.** New script `experiment/run_ablation_no_message_passing.py` + new `MLP` class in `gnn_training.py` (same `__init__`/`forward(data)` interface as GraphSAGE/GCN/GAT so it drops in via `model_class=MLP`, but `forward` only ever reads `data.x` — never `edge_index`/`edge_weight` — a genuine no-graph control on the identical 13-dim node vector SPIRE receives). Reuses `run_ablation.run_ablation_sweep`'s existing `model_class` parameter unmodified. **Verified**: `py_compile`; real smoke run (Middle East, 1 seed) — SPIRE F1=0.8909 vs MLP(no message passing) F1=0.8544, a genuine, non-trivial gap. Full sweep deferred. | `experiment/run_ablation.py`<br>`experiment/gnn_training.py`<br>`data/Global_REE_occurrence_database_processed.xlsx` | `result/ablation.json` (A, pending full sweep)<br>`result/ablation_no_message_passing.json` (C, pending full sweep) |
| **(7) No Explanation for SPIRE's Own Regional Variation.** SPIRE's own F1 (not GCN/GAT's) still varies sharply by region (0.742 North America → 0.910 South America, a 0.168 spread). The paper only analyzes why GCN/GAT fail in certain regions, not why SPIRE itself is weak in North America (0.742), South and Central Asia (0.806), China (0.788). Suggestion: add a discussion analyzing the geological and data characteristics of strong vs. weak regions. | Table 4 only reports F1 by region, with no accompanying covariate to explain it. Limitations mentions generally that "graph density differs... remote occurrences may retain only one or two neighbors" but never ties this to NA/China/South-Central Asia specifically. | **A. Region-specific error decomposition** — get TP/FP/FN/TN for each weak region vs. South America, analyze FN/FP by: (i) deposit-type (`is_group`), (ii) documentation completeness, (iii) mean $k$-NN distance of the misclassified nodes specifically. Directly answers "why is SPIRE weak here" instead of just describing a general correlation.<br><br>**B. Region diagnostics, contingent on A's outcome** — if A reveals one clearly dominant factor (e.g. most FN belong to a single deposit type), just compute that one covariate at the regional level (e.g. that deposit type's share) and check it against F1 to close the loop back to Table 4. If A is mixed with no standout factor, then run the full table: Region × F1 × $n$ × positive rate × deposit-type entropy (Comment 1-B) × documentation completeness (Comment 2-C.a) × mean $k$-NN distance (instead of avg node degree, since degree is nearly fixed by the constant $k=10/15$). | ✓ (contingent on 1, 2) | **A implemented (instrumentation + analysis script done, verified end-to-end with real 2-region data, full sweep deferred).** Instrumented `run_gnn.py` (Exp1) to save per-node data needed for error decomposition — `test_idx`/`y_true` per region, `y_prob`/`y_pred` per model under a new `predictions` key — purely additive. Kept `predictions` **separate** from `test_metrics` (not merged into it) since `_avg_metrics` sums every key of `test_metrics[name]` across regions to compute the cross-region average; a first attempt that put `y_prob`/`y_pred` inside `test_metrics[name]` broke that aggregation (`TypeError: unsupported operand type(s) for +: 'int' and 'list'`) — caught immediately by an actual smoke run (not just `py_compile`, which doesn't exercise this path), fixed by moving predictions to their own dict. New script `experiment/run_region_error_decomposition.py`: for each region, labels every test node TP/FP/FN/TN, then breaks the FN/FP set down by (i) deposit-type (`is_group`, read directly from the processed dataset via `test_idx`), (ii) documentation-completeness bucket (reuses `result/documentation_completeness_scores.json` from (2)-C.a via `ID_No`, no duplicated logic), (iii) mean k-NN distance (fresh haversine k=15 NearestNeighbors computed within each region's own node set, matching the graph's own `k`). Handles an older result file (missing the new per-node fields) gracefully — region skipped, reported, not a crash; confirmed against the current (pre-instrumentation) `result/gnn_inductive.json` (all 10 regions × 5 seeds correctly skipped, no error). **Verified with real data**: ran a real 2-region Exp1 seed (Middle East, Oceania) end-to-end through the full decomposition. Middle East: 63 test nodes (TP=50/FP=6/FN=5/TN=2), 11 misclassified, mean k-NN distance of misclassified nodes **414 km** vs **362 km** for correctly-classified ones. Oceania: 504 test nodes (TP=333/TN=68/FN=56/FP=47), 103 misclassified, mean k-NN distance **155 km** (misclassified) vs **103 km** (correct). **Preliminary finding (2 regions only, not yet the full sweep)**: misclassified nodes are consistently more spatially isolated than correctly-classified ones in both regions — directly ties Limitations' existing general remark ("remote occurrences may retain only one or two neighbors") to concrete per-region numbers instead of leaving it unquantified. Also notable: in Oceania, `is_placer` accounts for 45 of 103 misclassified nodes (~44%), a much larger share than any other deposit-type group — a candidate "one clearly dominant factor" per (7)-B's decision rule, though this needs confirming across all 10 regions/5 seeds before deciding between (7)-B's two branches (single-covariate closeout vs. full table). Full sweep (10 regions × 5 seeds, real per-node data replacing this 2-region smoke check) deferred to the end-of-Group-3 batch run — same reasoning as elsewhere: `run_gnn.py`'s per-node fields only need to exist, no separate "run for (7)" versus "run for the official rerun" distinction. | `data/Global_REE_occurrence_database_processed.xlsx`<br>`experiment/run_gnn.py`<br>`result/documentation_completeness_scores.json` | `result/gnn_inductive.json` (now includes `test_idx`/`y_true`/`predictions` per region, pending full rerun)<br>`result/region_error_decomposition.json` (pending full rerun) |
| **(8) No Actual Prospectivity Map.** The paper calls itself "prospectivity mapping" but has no concrete high/low-probability map, no geological interpretation of standout areas, no comparison against known mineral belts. | `fig:world_overview` only plots points by region + highlights the test region, not a continuous probability surface. Kriging/spatial $k$-NN are only used as metric baselines, never used to produce an interpretive map. No section compares against known mineral belts (Bayan Obo, Mountain Pass, South China IAC belt...). | **A.** Run inference over a grid of query points (using the existing one-node-at-a-time deployment protocol already built) to produce a probability map, overlaid via cartopy (reusing the `fig:world_overview` pipeline).<br><br>**B.** Pick 2-3 known mineral belts, check whether they fall in high-probability zones — prioritize locations *not* in the training set to avoid this just being memorization.<br><br>**C.** Compare against a published mineral-belt/metallogenic map as external validation.<br><br>**Dependency on Comment (2):** only pursue this if the strict-label sensitivity result in (2)-A comes back "robust" (the prospectivity claim holds); if (2)-A collapses, follow the (2)-E route instead (walk back the "prospectivity mapping" label) rather than building a map for a claim that's about to be narrowed. | | **Moot — not needed.** (10) was decided as **Recast**, not Extend: every self-claim of "REE prospectivity mapping" was already replaced with "cross-region classification of REE-bearing geological occurrence records" (see (10)'s Edited note). (8)'s premise — "the paper calls itself prospectivity mapping but doesn't deliver one" — no longer holds once the paper doesn't make that claim, so there's nothing left to fix here; this matches the Group 3 plan itself, which scoped (8) as "only if Extend is chosen at (10)." **Optional, not required:** the spirit of (8)-C (comparing model scores against known mineral belts) could still be reused as a voluntary illustration for (9) (geological interpretation) or (7) (explaining strong/weak regions) — but it's a value-add, not something the reviewer requires, so it's not prioritized. | text only | text only |
| **(9) Results Section Lacks Geological Interpretation.** Results are almost entirely numerical; apart from feature plausibility there is no other geological insight — contrary to what's expected at Natural Resources Research. | Only 1 paragraph of geological interpretation ("Geological Plausibility of Selected Features"); the remaining Discussion sections explain *why the GNN performs well* (architecture), not *what the results say about REE metallogeny*. | No new analysis needed — **integrate the results of (1), (7), (8) into the main narrative** instead of leaving them scattered: after each table/figure, add 2-3 sentences of geological interpretation. Do this last, as a final editing pass. | ✓ (contingent on 1, 7, 8) | | text only | text only |
| **(10) Definition and Scope of the Prediction Task.** Since the underlying dataset is already a database of REE deposits/occurrences, the current model classifies existing database records rather than directly estimating prospectivity for unsampled grid cells or arbitrary locations. The wording in Abstract/Highlights/Conclusion goes beyond what this experiment demonstrates. The reviewer suggests choosing one of two paths: (a) recast the study as classification of confirmed REE mineral information within occurrence records, or (b) extend it into a proper prospectivity-mapping setting with independently defined background/barren samples, continuous spatial prediction, and external validation. The title and main claims must be consistent with whichever direction is chosen. | Overlaps with (2) and (8) but broader: reviewer explicitly permits either branch ("either...or"), so this is a **decision point**, not just more experiments. | | | **Decided: Recast.** Replaced every self-claim of "REE prospectivity mapping" (Highlights, Introduction, pipeline figure caption, Data section, Results, Limitations, Conclusion) with "cross-region classification of REE-bearing geological occurrence records," plus a closing Conclusion sentence stating SPIRE classifies existing occurrence records rather than predicting prospectivity for unsampled locations. | text only | text only |
| **(11) Target leakage via host-lithology inference.** Section 3.2 uses `REE_Mins` (together with `Dep_Type` and `Sig_Mins`) to infer `Host_Lith` when missing, but `REE_Mins` is also the exact field that defines the response variable in Section 3.4 — creating a direct leakage path from target into input feature. Removing REE mineral names from `Sig_Mins` does not address the leakage coming through `Host_Lith`. Proposed fix: reconstruct `Host_Lith` without `REE_Mins` (using `Dep_Type`, cleaned non-REE `Sig_Mins`, or an independent geological source; uncertain cases → Unknown), rerun all main experiments, and report how results change. | Confirmed: Tier 1 of Host Lithology Inference directly uses diagnostic REE minerals from `REE_Mins` (pyrochlore→carbonatite/alkaline; bastnäsite/monazite→alkaline igneous/carbonatite...) — real target leakage, not just "uncertainty" as (2)-D treated it. More seriously: `Host_Lith` is leaked → the 9 `is_group` groups are leaked → not just the 2 features that survived SHAP (`_metasomatic`, `is_placer`) are affected, but the **graph edges** too, since `w_feature` (cosine similarity) is computed on the very vector containing these `is_group` values — spreading into the entire graph structure, used throughout all 6 experiments. (2)-D only ablates post-hoc on the already-leaking pipeline, doesn't fix it at the source, and doesn't call for rerunning the main experiments. | **A. Fix the rule-based inference at the source (Section 3.2)** — drop `REE_Mins` entirely from Tier 1; use only `Dep_Type` keywords + cleaned `Sig_Mins` (REE mineral names removed, same as done in Section 3.4) or an independent geological source. Cases previously inferable only via Tier 1 (REE mineral) → assign Unknown/unclassified, reusing the existing fallback.<br><br>**B. Rebuild the entire downstream pipeline** — `is_group` (9 groups) from the corrected `Host_Lith`, rebuild both the node feature vector AND the graph edges.<br><br>**C. Rerun all 6 main experiments** with the corrected pipeline, report the delta versus the old numbers (Table 4, ablation, proportion prediction...).<br><br>**Link to (2)-D**: once (11) is fixed at the source, the confidence-tier ablation in (2)-D can remain as a secondary robustness check on the now-clean pipeline, but it's no longer a way to "detect" leakage since the leakage has been removed at its root. | | **A done (code only, not yet rerun).** In `preprocessing/host_lith.py::infer_host_lith`, removed the `ree` variable (`REE_Mins`) and all 4 branches that used it (Dep_Type-empty tier, "OTHER IGNEOUS", "FLUORITE DEPOSIT", final "unclassified" fallback). Sig_Mins-based and Dep_Type-based branches untouched — they don't reference REE-diagnostic mineral names, so no further cleaning of Sig_Mins was needed. Verified with `py_compile` (syntax OK) + 3 sanity calls to `infer_host_lith` confirming REE_Mins-only cases now fall through to sig/unclassified instead of returning an REE-derived lithology. **B done.** Ran `python preprocessing/pipeline.py` end-to-end with the fixed `host_lith.py`: Host_Lith filled for 1134 records (no `REE_Mins`), `is_group` rebuilt, XGBoost SHAP top-10 selected (`has_uraninite, has_thorite, has_fluorite, has_magnetite, has_pyrochlore, has_ilmenite, Latitude, is_placer, Longitude, has_zircon`), node vector 11-dim, graph rebuilt (k=15, 93,360 edges, avg degree 30). Wrote updated `data/Global_REE_occurrence_database_processed.xlsx`, `host_lith_stats.xlsx`, `has_dash_records.xlsx`, `gnn_features.parquet`, `gnn_target.npy`, `gnn_edge_index.npy`, `gnn_edge_weight.npy`. **C (rerun all 6 main experiments) not yet done** — pending. | `data/Global_REE_occurrence_database.xlsx` (raw)<br>`preprocessing/host_lith.py`<br>`preprocessing/pipeline.py` | `data/Global_REE_occurrence_database_processed.xlsx`<br>`data/host_lith_stats.xlsx`<br>`data/has_dash_records.xlsx`<br>`gnn_features.parquet`<br>`gnn_target.npy`<br>`gnn_edge_index.npy`<br>`gnn_edge_weight.npy` |
| **(12) How the ensemble probability is constructed for training nodes.** The paper doesn't explain how the ensemble probability is generated for training nodes — if it uses fitted probability from a model fit on those exact observations, the graph model may inherit in-sample overfitting (important because Fig 10 shows a sharp F1 drop when removing the ensemble prior). Proposed fix: use K-fold out-of-fold for training nodes, refit on the full outer-train set to score val/test nodes, report all 3 conditions (OOF/in-sample/no-ensemble), and clearly describe the order of feature selection/tuning/probability generation. | Code (`experiment/run_ablation.py:85-116`, `preprocessing/feature_engineering.py:466`) confirms: XGBoost/CatBoost/RF are fit only on `train_idx`, `predict_proba` is called on `tv_pos` (train_idx + val_idx) — validation nodes get valid out-of-sample scores, **training nodes get in-sample scores**. | **A.** For each outer training split, generate the ensemble score for training nodes via K-fold out-of-fold (e.g. K=5): split `train_idx` into K folds, for each fold train on the remaining K-1 folds and predict the held-out fold.<br><br>**B.** Refit the 3 tabular models on the full `train_idx` to score val/test nodes (current logic is already correct here, just needs to be stated explicitly in the text).<br><br>**C.** Report 3 parallel conditions: out-of-fold probabilities vs. fitted in-sample probabilities vs. no ensemble prior.<br><br>**D.** Clearly describe the order of feature selection (SHAP) → tuning → OOF probability generation → graph construction in the Methodology. | | **A implemented (code only, not yet wired into experiment scripts).** Added `compute_ensemble_scores_no_leakage()` + `_make_ensemble_models()` to `preprocessing/feature_engineering.py`: K-fold (default 5, `StratifiedKFold`) OOF scoring for `train_idx`, refit-on-full-`train_idx` scoring for `other_idx` (val/test) — same XGBoost/CatBoost/RF hyperparameters used everywhere else in the repo. Verified with `py_compile` + a synthetic-data sanity test: in-sample (old/leaky) AUC = 1.000 (the exact overfitting artifact the reviewer flagged), OOF (fixed) AUC = 0.931, other_idx refit AUC = 0.872 — same order of magnitude, no leakage signature. **B confirmed unchanged/correct**: the val/test scoring path (refit on full train_idx) was already leakage-free, now reused via `fitted_models` return value. **A wired into all call sites.** Replaced the leaky in-sample block (fit CatBoost/RF on `train_idx`, `build_node_features` on `train_idx+val_idx`) with `build_node_features_no_leakage(...)` in `run_ablation.py`, `run_ablation_exp3.py`, `run_exp4_proportion.py` (2 call sites), `run_gnn_per_region.py`, `run_exp5_proportion_per_region.py`, `run_hparam_sensitivity.py`, `run_gnn.py`, `run_k_sensitivity.py`, `run_inductive_sample.py`. `run_exp6_proportion_inductive_sample.py` needed no change — it reuses `run_inductive_sample._build_graph_artifacts`, already fixed. Where the pre-fit XGBoost (`xgb_model`) was also reused for `build_baseline_features` (tabular baselines), that usage was left untouched — only its reuse as an ensemble member was removed, since the new function fits its own fresh models. Single-node test-time inference calls (using the refit-on-train `ensemble_models`, already leakage-free per Solution B) were left as-is. Also dropped an unused `"xgb_model"` dict key in `run_gnn.py`/`run_inductive_sample.py` (never read downstream). **Verified**: `py_compile` on all `experiment/*.py`; all 10 modules import cleanly; grep confirms no dangling references to the removed `cat_model`/`rf_model`/`X_tr`/`y_tr` variables; end-to-end smoke runs on the real (post-step-B) dataset — `run_ablation._build_graph_artifacts("South and Central Asia", mode="full")` (2580 nodes) and `run_hparam_sensitivity._build_region_graph("Oceania")` (499 nodes) both complete and return correctly-shaped node features (11-dim) and 3 fitted ensemble models. **C — code path implemented, run deferred.** Added `train_scoring` flag (`"oof"` default / `"in_sample"`) to `compute_ensemble_scores_no_leakage` and `build_node_features_no_leakage` in `feature_engineering.py`: `"in_sample"` reproduces the exact pre-fix leaky behaviour (train nodes scored by the same models fit on them) for comparison only — `other_scores` is identical either way (unaffected, confirmed by test). Threaded through `run_ablation.py` (`_build_graph_artifacts`, `run_ablation_sweep`, new `--train-scoring` CLI flag, both backward-compatible/default-`"oof"`). New script `experiment/run_ablation_ensemble_scoring.py` runs all 3 conditions (oof/in_sample on mode=no_edge_stats 11-dim, no_ensemble on mode=no_ensemble 10-dim — edge-stat mode deliberately excluded to keep this isolated from the separate Comment 13 double-counting issue) and writes `result/ablation_ensemble_scoring.json`. **Verified**: `py_compile`; unit test confirms `in_sample` reproduces AUC=1.000 (matching the original leaky artifact) while `other_scores` stays identical across both modes; smoke run (1 region "Oceania", 1 seed, all 3 conditions) completes end-to-end — F1: oof=0.893, in_sample=0.879, no_ensemble=0.812. **Full run (10 regions × 5 seeds × 3 conditions, ~2.5h) intentionally deferred** — user decided to run this once at the end, after (13)/(14) are also fixed, alongside the full 6-experiment rerun, rather than now on a partially-fixed pipeline. **D (Methodology text) not yet done.** **Addendum, found while investigating Comment 17 (fairness):** the same in-sample-scoring bug this comment fixed for SPIRE's `ensemble_score` also existed, unpatched, in the tabular baselines' own `xgb_score` feature (`build_baseline_features()` called `xgb_model.predict_proba(X)` where `xgb_model` was fit on `train_idx`, scoring train rows in-sample) — outside Comment 12's original scope (which only covered the GNN "ensemble" path) but the same root cause. **Fixed**: replaced the single-XGBoost in-sample score with the same `compute_ensemble_scores_no_leakage` OOF machinery, generalized with a new `models_factory` param (default `_make_ensemble_models`, or the new `_make_single_xgb_model` for a single-model score) so the fix is reused rather than duplicated. Wired into all 6 baseline-touching files: `run_baseline.py`, `run_baseline_per_region.py`, `run_exp4_proportion.py`, `run_exp5_proportion_per_region.py`, `run_exp6_proportion_inductive_sample.py`, `run_inductive_sample.py`. **Verified**: `py_compile` + import on all 6; unit test confirms `models_factory=_make_single_xgb_model` reproduces the same in-sample AUC=1.000 artifact when `train_scoring="in_sample"` and a realistic OOF AUC otherwise; real end-to-end runs on Middle East for all 6 files complete without error (baseline-only and mixed baseline+GNN functions alike). See the new self-discovered comment for the related split-reseeding fix. **Second addendum, found while executing the full rerun (real crash, reported by user) on `run_gnn_per_region.py --seeds 42 50 32 17 21`:** Comment (4)-B's spatial buffer (excludes candidates within `buffer_km` of the test set) can shrink an already-small, class-imbalanced region's train/val pool enough that `compute_ensemble_scores_no_leakage`'s `StratifiedKFold(n_splits=5)` OOF loop produces a fold whose training portion has zero examples of the minority class — XGBoost's sklearn API then raises `ValueError: Invalid classes inferred from unique values of y. Expected: [0], got [1]` (real case: Middle East/seed=50, buffer excluded 35 of 54 remaining candidates, leaving only 16 train nodes with 1 minority example). **Fixed** in `compute_ensemble_scores_no_leakage()`: compute `min_class_count` in `y_train`; if `<2`, print a warning and fall back to in-sample scoring for train nodes only (reusing the existing `train_scoring="in_sample"` path — `other_idx` stays leakage-free as always); otherwise cap `n_folds` at `min(n_folds, min_class_count)` with a warning if reduced. Since this lives inside the shared function, it protects every caller (all Group 1/3 scripts) without separate patches. **Verified**: 3 isolated synthetic-data unit tests (exact 1-minority-example degenerate case, a reduced-folds 3-minority-example case, and a normal balanced case unaffected) all pass; re-ran the actual real failing command's scenario directly (`run_gnn_per_region._run_region(df, "Middle East", random_seed=50)`) — no crash, correct warning printed (`minority class has only 1 example(s) among 16 train nodes`), sane output (spire f1=0.8, gcn f1=0.8, gat f1=0.0). **User then hit the same error on Exp5** (`run_exp5_proportion_per_region.py`, which reuses the exact same shared `compute_ensemble_scores_no_leakage`/`build_node_features_no_leakage` functions) — confirmed already covered by the shared fix: ran a full real sweep (10 regions × Exp5's own 5 README seeds `[42,18,20,48,50]`, `k_for_buffer=10`), 0 crashes across all 50 (region, seed) combinations. | `preprocessing/feature_engineering.py`<br>`experiment/run_ablation.py` + 9 other experiment scripts (wired call sites)<br>`data/Global_REE_occurrence_database_processed.xlsx` | `result/ablation_ensemble_scoring.json` (smoke run only, full sweep deferred) |
| **(13) Graph construction reuses the same information twice.** Lat/long and `ensemble_score` are each used once directly (spatial edge weight / node feature) and then reappear inside the feature vector used for cosine similarity — double-counting information, potentially amplifying the tabular model's output. Needs: exclude lat/long + `ensemble_score` from the geological-similarity term; compare spatial-only/geological-only/combined; empirical support for the 0.5/0.5 weighting, $k$, and the 2000km threshold; graph diagnostics. | Confirmed via code: the 11-dim vector used to compute `w_feature` does contain `Latitude`, `Longitude`, `ensemble_score` — the double-counting is real. "$k$ needs empirical support" is already covered by `fig:k_sens` — nothing further needed there. The 0.5/0.5 weighting, the 2000km threshold, and graph diagnostics have never been tested. | **A.** Fix `w_feature`: drop `Latitude`/`Longitude`/`ensemble_score` from the vector used for cosine similarity, keep only purely geological mineral/lithology indicators.<br><br>**B.** Rebuild the graph and rerun 4 conditions: spatial-only, geological-only (fixed), current 0.5/0.5 combination (still double-counting), fixed 0.5/0.5 combination — giving empirical support for the 0.5/0.5 weighting.<br><br>**C.** Sweep the distance threshold (e.g. 500/1000/1500/2000/2500/3000km) the same way as was done for $k$, report F1/ROC-AUC at each threshold.<br><br>**D.** Add graph diagnostics: degree distribution by region (links to (7)-B), count/list isolated/near-isolated nodes, edge-weight histograms (spatial/feature/combined) — run on the graph fixed in A.<br><br>**Link to (6)**: the "edge-stat" contribution (0.039 F1) needs to be rerun on the graph fixed in A — part of it may just be `ensemble_score` echoed back through the graph, not independent relational signal. | | **A implemented and wired everywhere (code only, not yet rerun).** Added `X_geo` param to `build_weighted_graph()` in `feature_engineering.py`: cosine similarity now uses `X_geo` (when given) instead of `X_node`, defaulting to the old behaviour when omitted for backward compatibility. Added `select_geo_similarity_columns()` helper — drops `Latitude`/`Longitude` from the SHAP-selected columns (`ensemble_score` is never part of `X_selected`, so it's excluded automatically). Wired `X_geo` into **all 16 call sites across 10 files**: `run_ablation.py`, `run_ablation_exp3.py`, `run_exp4_proportion.py` (×2), `run_exp5_proportion_per_region.py`, `run_gnn.py`, `run_gnn_per_region.py`, `run_inductive_sample.py`, `run_k_sensitivity.py`, `run_hparam_sensitivity.py`, `preprocessing/pipeline.py` — both the TV-graph build (training) and the single-node mini-graph build (test-time inductive inference), keeping train/test consistent. Test-time functions (`_predict_node`/`_predict_node_aug`, shared across several files) gained a new `x_new_geo` parameter; artifacts dicts gained `X_geo_tv_base`. Also removed a stale, never-read `"xgb_model"` dict key found along the way (unrelated cleanup). **Verified**: unit test confirms `w_geo`/edge_weight changes when `X_geo` differs from `X_node`; `py_compile` on all touched files; all 10 experiment modules import cleanly; real end-to-end smoke runs — `run_ablation._build_graph_artifacts`+`_train_and_predict` (Oceania, mode="full", 504 test nodes) and `run_gnn_per_region._run_region` (Middle East, all 3 GNN models spire/gcn/gat) both complete with sane, non-degenerate metrics; `preprocessing/pipeline.py`'s graph-build logic verified in isolation (no files overwritten) — `X_geo` shape (3112, 8) vs `X_node` (3112, 11), confirming Latitude/Longitude/ensemble_score are excluded. **B — code implemented, verified, full run deferred.** Added `weighting_strategy`/`geo_fix`/`model_class` params to `_build_graph_artifacts`/`run_ablation_sweep`/`_train_and_predict` in `run_ablation.py` (all default to current behaviour, backward-compatible). `geo_fix=False` deliberately reuses the full node vector as `X_geo` (reproducing the pre-fix double-counting) for comparison only. New script `experiment/run_ablation_graph_weighting.py` runs the 4 named conditions (spatial_only / geo_only / mixed_current / mixed_fixed) on mode="no_edge_stats", writes `result/ablation_graph_weighting.json`. **Major finding surfaced while building this (not one of the 18 cataloged comments):** `SAGEConv` (SPIRE/GraphSAGE) and the `GATConv` usage in `gnn_training.py` do **not** consume `edge_weight` in message passing at all (confirmed against installed PyG 2.6.1's `SAGEConv.forward` signature — no `edge_weight` param exists). Only `GCNConv` (the GCN baseline) does. Concretely: `GraphSAGE.forward()`/`GAT.forward()` call `conv(x, edge_index)`, dropping `edge_weight`; `GCN.forward()` calls `conv(x, edge_index, edge_weight)`. So the entire weighted-graph mechanism this comment (and (14)) revolves around has **zero direct effect on SPIRE's own predictions** in mode≠"full" — confirmed empirically: all 4 conditions produced byte-identical training curves/probabilities on SPIRE (Middle East smoke test). The only channel by which edge weights reach SPIRE is indirectly via `augment_with_edge_stats` (`w_mean`/`w_max`, mode="full" only). User decision: run the (13)-B comparison on **GCN** instead of SPIRE (`model_class=GCN`, the only architecture where it's informative) rather than pausing or filing this as a separate tracked problem. **Verified on GCN**: smoke run (Middle East, 1 seed) gives genuinely different F1 per condition — spatial_only=0.779, geo_only=0.832, mixed_current(buggy)=0.940, mixed_fixed=0.876 — confirming the comparison now works as intended. **C — code implemented and verified.** New script `experiment/run_max_dist_sensitivity.py` (mirrors `run_k_sensitivity.py`'s structure exactly, but sweeps `max_distance_km` instead of `k`; reviewer's own list {500,1000,1500,2000,2500,3000}km, k=15/alpha=0.5 fixed, mode="full", SPIRE). Unlike B, `max_distance_km` changes edge **topology** (which neighbours survive the adaptive-k filter), not just edge weight — so it genuinely affects SPIRE directly (confirmed: n_edges differed 82,672 vs 91,470 for 500km vs 3000km on one region; a direct train+predict smoke test on Middle East gave different val F1 and test F1 for the two thresholds). Writes `result/max_dist_sensitivity.json`. **D — code implemented and verified.** New script `experiment/run_graph_diagnostics.py`: builds one full-dataset graph (same construction as `preprocessing/pipeline.py`, Comment 13-A fix applied, `max_distance_km=2000` — the same threshold used everywhere else, needed to make isolation realistic), then reports (1) degree distribution by region, (2) isolated/near-isolated node counts, (3) edge-weight histograms for spatial/geo/combined (calls `build_weighted_graph` 3× with different `weighting_strategy`, same topology). Writes `result/graph_diagnostics.json` + a 3-panel PNG histogram plot. **Verified with a real run**: all 10 regions show healthy mean degree (~29-31, matching k=15 plus incoming edges); 0 fully isolated nodes; 1 near-isolated node (degree=1) in South America once the 2000km threshold is applied — matching (7)'s "remote occurrences may retain only one or two neighbours" concern; geo-only histogram shows a bimodal spike near 0 and near 1 (sparse binary mineral indicators → mostly no overlap or near-identical profiles), visibly different from the smoother spatial/combined histograms. **Full run (all regions × 5 seeds for B/C, full dataset already run once for D) deferred** to the end of Group 1 alongside (14) and the 6-experiment rerun, per the earlier decision. | `preprocessing/feature_engineering.py`<br>`data/Global_REE_occurrence_database_processed.xlsx` | `result/ablation_graph_weighting.json`<br>`result/max_dist_sensitivity.json`<br>`result/graph_diagnostics.json` (+ PNG histogram plot) |
| **(14) Different edge rule for train vs. held-out.** Train-train edges use both spatial + feature similarity; edges touching val/test nodes use spatial only. Since `w_mean`/`w_max` are node features computed from edge weights, train and held-out nodes receive features from two different rules — a possible train-test distribution shift. Proposed: use one consistent rule (a new node's observable features can still be used at prediction time, as long as the target isn't used); if the current design is kept, compare it against a unified rule and show the distribution of neighborhood size, `w_mean`, `w_max` between train and held-out. | `main.md:532-535` confirms exactly this: "combined weight applied only to edges whose endpoints are both training nodes... edges incident to validation or test nodes fall back to spatial term alone." This is the third bug tied to `w_mean`/`w_max`, after (12)/(13) — but a different mechanism: not leakage/double-counting, but an asymmetric rule between train and held-out. | **A (recommended).** Use one unified rule for every edge — apply the same spatial+feature formula (fixed in (13)) to both train-train edges and edges touching val/test, since `w_feature` depends only on $x$ (observable features), not $y$ (target) — no leakage risk from using it on held-out nodes.<br><br>**B (if the current design is kept).** Run the unified rule (A) and the asymmetric rule (current) in parallel, report F1/ROC-AUC/PR-AUC for both to justify it empirically.<br><br>**C.** Plot/compare the distribution of neighborhood size, `w_mean`, `w_max` between train and held-out nodes (add to the graph diagnostics in (13)-D) to quantify the extent of the shift, whichever branch is chosen. | | **A implemented (code only), B and C implemented and run — still a decision pending.** Added `unify_edge_rule: bool = False` to `build_weighted_graph()` in `feature_engineering.py`: default `False` keeps the current shipped (asymmetric) behaviour unchanged — this is an open "choice" item, not a confirmed bug like (13), so no existing caller's default was flipped. `True` skips the `both_fit` gating entirely, applying the same spatial+geo formula (using the (13)-A geo-only fix) to every edge. Threaded through `_build_graph_artifacts`/`run_ablation_sweep` in `run_ablation.py`. Only the main TV-graph build needed the flag — the mini-graph (test-time inductive prediction) never passed `fit_idx` in the first place, so it was already applying the unified rule by omission; the asymmetry only ever affected train-vs-val edges inside the TV graph. New script `experiment/run_ablation_edge_rule.py` (Solution B) runs asymmetric vs unified in parallel on mode="full" (13-dim, WITH edge-stat augmentation — the channel this comment is actually about) on SPIRE directly (no GCN workaround needed here, since Comment 14 is about a node-feature distribution shift, not message-passing weights). Comment 14-C added directly into `run_graph_diagnostics.py` (`run_train_val_shift_diagnostics`, reuses `_build_graph_artifacts` rather than rebuilding the pipeline) — compares degree/`w_mean`/`w_max` between train and val nodes under both conditions. **Verified with real runs**: (B) smoke test (Middle East, 1 seed) — asymmetric F1=0.835, unified F1=0.891, genuinely different. (C) real run (Middle East) — **asymmetric shows a real train/val gap** (w_mean 0.480 train vs 0.517 val; w_max 0.799 vs 0.844), **unified narrows it substantially** (w_mean 0.468 vs 0.459; w_max 0.718 vs 0.709) — degree distribution identical between conditions as expected (topology unaffected, only edge weight changes). This is concrete empirical evidence in favour of Solution A, but **Decided: A (unified rule).** Based on the (14)-B/C evidence above (unified reduces the train/val gap and scores higher F1, with no leakage risk either way), flipped the default: `unify_edge_rule` now defaults to `True` in `_build_graph_artifacts`/`run_ablation_sweep` (`run_ablation.py`). Wired `unify_edge_rule=True` explicitly into the main TV-graph `build_weighted_graph` call in the other **9 files**: `run_ablation_exp3.py`, `run_exp4_proportion.py` (×2 call sites), `run_exp5_proportion_per_region.py`, `run_gnn.py`, `run_gnn_per_region.py`, `run_inductive_sample.py`, `run_k_sensitivity.py`, `run_hparam_sensitivity.py` — this now applies to all 6 main experiments, not just the ablation/comparison scripts. `run_ablation_edge_rule.py` (14-B) still explicitly overrides both conditions regardless of this default, so its comparison capability is unaffected. **Verified**: `py_compile` on all 9 files; all import cleanly; grep confirms no `fit_idx=`-bearing call site was left without `unify_edge_rule`; real end-to-end smoke run on `run_gnn.py` (the actual Exp1 script) — `_build_graph_artifacts` + full train (Middle East) completes without error, val F1=0.908, sane. **Practical consequence: all 6 main experiments (Exp1-6) now need to be (re)run once, since none of their result files reflect this new default yet** — this is exactly the "rerun 6 experiments" step at the end of Group 1, now unblocked since (14) is no longer an open decision. | `preprocessing/feature_engineering.py`<br>`experiment/run_ablation.py` + 9 other experiment scripts<br>`data/Global_REE_occurrence_database_processed.xlsx` | `result/ablation_edge_rule.json`<br>`result/graph_diagnostics.json` (14-C added into the same file as 13-D) |
| **(16) Unseen region vs. unseen node.** Exp1 withholds an entire region — a genuine "unseen-region" test. Exp3/Exp6 only withhold nodes from regions still represented in training — "unseen nodes within an already-represented region," not unseen-region. Fig 7's caption and several other statements don't distinguish the two. Consistent terminology is needed throughout Abstract/Highlights/Methods/captions/Discussion/Conclusion: "cross-region"/"unseen-region generalization" reserved for leave-one-region-out; Exp3/Exp6 should be called "global inductive prediction of held-out nodes." | Overlaps in spirit with (4)-F ("prioritize Exp1 for cross-region claims") but much narrower — (4) only covers node-level classification (Exp1/2/3), doesn't touch Exp4/Exp6 (proportion prediction) or call for a manuscript-wide audit. One concrete error confirmed: the `fig:res_A1` caption (main.md:857-858) reads "(c) global inductive proportion prediction (Exp 6) evaluating on **previously unseen geographic areas**" — wrong, since that's a property of Exp4, not Exp6. | **A.** Fix the `fig:res_A1` caption: "evaluating on previously unseen geographic areas" → "evaluating on previously unseen nodes within regions represented in training".<br><br>**B.** Audit the whole manuscript: keep "cross-region"/"unseen-region generalization" only for Exp1/Exp4; change every description of Exp3/Exp6 to "global inductive prediction of held-out nodes"; also check `fig:res_B1`, `fig:res_C1`. | ✓ | **A done.** `fig:res_A1` caption fixed: "...(Exp 6) evaluating on previously unseen geographic areas" → "...(Exp 6) evaluating on previously unseen nodes within regions represented in training." **B done.** Audited every "cross-region"/"unseen region"/"global inductive" mention in `main.md`. `fig:res_A`, `fig:res_B`, `fig:res_C`, `fig:res_B1`, `fig:res_C1`, and all Results/Discussion prose already correctly separate "leave-one-region-out (Exp 1/4)" from "global inductive (Exp 3/3a/6)" — the `fig:res_A1` caption was the only actual error found. No further text changes needed. | text only | text only |
| **(17) Fairness of baseline comparisons.** SPIRE receives a 13-dim input, tabular baselines only use "selected tabular features" — impossible to isolate the contribution of message passing from that of the added variables. GCN/GAT aren't tuned independently. Needed: an MLP on the same 13-dim input, a no-message-passing control, an ensemble-only model, controls with edges removed/randomly rewired; GCN/GAT should be tuned fairly or clearly presented as a backbone ablation; the Conclusion shouldn't imply general superiority without evidence. | Code (`feature_engineering.py:472-482`) confirms baselines use an 11-dim input (10 SHAP + a single `xgb_score`) while SPIRE uses 13-dim — the code even has a comment wrongly claiming "same as GNN." The GCN/GAT-not-independently-tuned point has already been handled the way the reviewer accepts as a fallback: Limitations already states this is a controlled comparison of the aggregation rule under a shared setup — just needs a Conclusion audit. | **A.** Fix `build_baseline_features` so the MLP uses the exact same 13-dim input as SPIRE.<br><br>**B.** No-message-passing control — reuse (6)-C, using the 13-dim input from A.<br><br>**C.** Ensemble-only baseline: threshold `ensemble_score` directly (after the OOF fix in (12)).<br><br>**D.** Random-rewire control: keep the node features + degree distribution, randomly shuffle the connections.<br><br>**E.** Audit the Conclusion, remove any sentence implying general superiority without direct supporting evidence. | | **A — merged into (6)-C, not a separate baseline-pipeline change.** "An MLP using the exact same 13-dim input as SPIRE" is exactly what (6)-C's `run_ablation_no_message_passing.py` already builds (SPIRE's own mode="full" node vector fed through a plain `MLP`) — chose to satisfy A this way rather than editing `build_baseline_features`/the official Table 4 "mlp" baseline row, since that would force a rerun of the main 6 experiments for no added evidentiary value (the standalone comparison already isolates the same thing A asks for). **B implemented and verified (smoke-tested, full sweep deferred) — same experiment as (6)-C, see that row for the full write-up.** SPIRE F1=0.8909 vs MLP(no message passing) F1=0.8544 on the Middle East smoke test. **C implemented (code done, smoke-tested, full sweep deferred).** New script `experiment/run_ablation_ensemble_only.py`: thresholds the already-fitted `ensemble_score` (mean of OOF XGBoost/CatBoost/RandomForest, Comment 12's leakage-free fix) at 0.5, no graph/message-passing involved, compared against SPIRE on the same Exp1 (leave-one-region-out) split. Reuses `run_ablation.run_ablation_sweep` unmodified for SPIRE's numbers; the ensemble-only baseline reads directly off `_build_graph_artifacts`'s cached `ensemble_models`/`X_test` (same non-graph-baseline pattern already used in `run_lodto.py` for (1)-C). Since `run_ablation.py`'s existing design builds the split/ensemble once per region (only GNN training varies across seeds), the ensemble-only baseline is deterministic per region — computed once per region, not once per (region, seed). **Verified**: `py_compile` + import; real smoke run (Middle East, 1 seed) — SPIRE F1=0.8909 vs ensemble-only F1=0.8317 (delta +0.0592), a genuine, non-trivial gap confirming the comparison captures a real effect. **D implemented and verified (smoke-tested, full sweep deferred).** New script `experiment/run_ablation_random_rewire.py`: degree-preserving random rewiring via `networkx`'s `double_edge_swap` (unit-tested in isolation: degree sequence exactly preserved, ~80% of edges changed, edge-weight distribution preserved as a multiset but reassigned to the new pairs) applied to the training graph; both `tv_data.edge_index`/`.edge_weight` (training) AND `graph_artifacts["edge_index_tv"]`/`["edge_weight_tv"]` (reused directly as the base graph for test-time inductive augmentation) are rewired together, so training and test-node prediction stay consistent — rewiring only one would silently train on one graph and predict on another. A new inductive test node's own edges to its k nearest neighbours still use its real coordinates/features (can't meaningfully rewire a single new node's own connections in isolation); only the existing training-graph topology it attaches to is randomized — documented as a scope limitation. **Verified**: `py_compile`; real smoke run (Middle East, 1 seed) — real_graph F1=0.8317 vs random_rewired F1=0.8544 (single-region/single-seed, not interpretable on its own — just confirms the code path is not a no-op). **E — text only, not yet written; do last, after A-D's full results are in.** Full sweeps for A/B/C/D (10 regions × 5 seeds each) deferred to the end-of-Group-3 batch run. | `preprocessing/feature_engineering.py`<br>`experiment/run_ablation.py`<br>`experiment/gnn_training.py`<br>`data/Global_REE_occurrence_database_processed.xlsx` | `result/ablation_no_message_passing.json` (A/B, pending full run)<br>`result/ablation_ensemble_only.json` (C, pending full run)<br>`result/ablation_random_rewire.json` (D, pending full run) |
| **(18) Meaning of regional proportion prediction.** Section 3.8 computes proportion as the mean predicted probability across test records, compared against the observed fraction. The USGS database isn't a spatially representative sample — this number cannot be read as true regional deposit density/resource potential. Needs a reframed task description, calibration checks, or independent validation for a stronger interpretation. | Same root cause as (10) but specific to Section 3.8 (Exp4-6), which (10) doesn't touch. Confirmed: `main.md:691-701` has no representativeness caveat, and there's no Brier score/calibration plot anywhere. | **A.** Reframe the task description as "estimation of positive fraction among sampled records" — parallel to the Recast move in (10).<br><br>**B.** Add a Brier score + reliability plot (run on predictions after (12)/(13)/(14) are fixed).<br><br>**C (optional, only if (10) chooses Extend).** Independent validation against deposit statistics/grid samples/external surveys.<br><br>**D (if C isn't pursued).** Add an explicit sentence to the Discussion: MAE/RMSE only reflect the sampled-record fraction, not true density. | ✓ (after 12, 13, 14 — see §2) | **A done** — edited in `## Training and Evaluation Strategy` (`#sec:Graph_Construction` follow-on section, where region-level proportion is first defined), replacing the paragraph starting "For region-level proportion prediction, the predicted REE occurrence proportion was calculated..." (formerly lines 691-701). Task now described as estimating "the positive fraction of REE-bearing records among the sampled test nodes," and a closing sentence added: because the USGS compilation is a curated database rather than a spatially exhaustive survey, the quantity should be read as sampled-record fraction, not true regional deposit density/resource potential.<br><br>**C dropped** per the Recast decision at (10) (same reasoning as dropping (8)). **B — instrumentation implemented and verified, analysis script implemented and verified, full run deferred (waiting for the post-Group-1 6-experiment batch rerun).** Added `y_true` (per region) and `y_prob` (per model) to the saved output of `run_exp4_proportion.py`, `run_exp5_proportion_per_region.py`, `run_exp6_proportion_inductive_sample.py` — purely additive, no existing metric/logic touched (previously only the aggregate `predicted_proportion`/`abs_error` was kept, discarding the per-node detail calibration needs). New script `experiment/run_calibration_analysis.py`: pools (y_true, y_prob) across every region/seed per model (handles both the dict-keyed-by-seed schema used by exp4 and the list-of-runs schema used by exp5/exp6), computes Brier score (mean squared error) and a 10-bin reliability/calibration curve per experiment × model, saves `result/calibration_analysis.json` + `figures/fig_calibration.png`. **Verified**: `py_compile`; unit tests on synthetic data confirm Brier score is correct at both extremes (near-perfect predictions → Brier≈0.01, worst-case predictions → Brier=1.0) and that reliability bins correctly reproduce a known-calibrated synthetic distribution; a real smoke call to each script's `_run_region` (Middle East, 1 seed) confirms `y_true`/`y_prob` populate correctly and consistently-sized across all 9-12 models per experiment. Ran the full `run_calibration_analysis.py` against the *current* (pre-instrumentation) `result/exp4-6*.json` files to confirm graceful degradation: correctly detects 50/50/50 region-runs missing `y_true` in each file, reports "no usable pairs," writes an empty (not broken) `result/calibration_analysis.json`, skips the plot with a clear message — no crash. **Full run intentionally deferred**: Exp4 is leave-one-region-out (unaffected by (4)), but Exp5/Exp6 are exactly what (4)-A/B would redesign if adopted — running the real sweep now risks the pooled data becoming stale and needing a redo; will run once as part of the post-Group-1 batch 6-experiment rerun.<br><br>**D done** — no longer conditional on C (D applies unconditionally now that C is dropped). Edited in `### Impact of Global Graph Connectivity on Regional-Scale Prediction` (Discussion), right after the sentence ending "...better agreement with the observed regional REE proportions under the global inductive setting." and before "To validate the contribution of each feature component...": added *"These MAE and RMSE values quantify agreement between predicted and observed positive-record fractions within the sampled test records under the adopted split; they should not be read as estimates of true regional deposit density or resource potential, since the underlying USGS compilation is not a spatially exhaustive survey."* | `experiment/run_exp4_proportion.py`<br>`experiment/run_exp5_proportion_per_region.py`<br>`experiment/run_exp6_proportion_inductive_sample.py`<br>`data/Global_REE_occurrence_database_processed.xlsx` | `result/exp4_proportion.json`, `result/exp5_proportion_per_region.json`, `result/exp6_proportion_inductive_sample.json` (now include `y_true`/`y_prob`, pending full rerun)<br>`result/calibration_analysis.json` (pending)<br>`figures/fig_calibration.png` (pending) |
| **(19) [Self-discovered, not a reviewer comment] Baseline train/val split not re-randomized across the "5 seed runs".** Found while investigating Comment 17 (fairness): SPIRE's own sweep scripts (`run_gnn.py`, `run_gnn_per_region.py`, etc.) rebuild the whole pipeline — including `create_spatial_split_indices(..., random_seed=seed)` — fresh for each of the 5 "seed runs", so the train/val split genuinely differs run to run. Two baseline-side scripts did not: they called `create_spatial_split_indices(processed_df, test_region=region)` with **no `random_seed=seed`**, so all 5 "runs" silently reused the exact same (default seed=42) split — only the downstream models' own `random_state` varied. This means the reported "avg ± std over 5 runs" measures a different, narrower source of variance for those baselines than it does for SPIRE, so the two "5-run" numbers are not directly comparable the way the paper implies. | Confirmed via code: `run_baseline.py`'s `_build_region_inputs()` (called once, outside the seed loop) and `run_exp4_proportion.py`'s `_run_region()` both omitted `random_seed=seed`. The other 4 baseline-touching scripts (`run_baseline_per_region.py`, `run_exp5_proportion_per_region.py`, `run_exp6_proportion_inductive_sample.py`, `run_inductive_sample.py`) already threaded the seed correctly — no bug there. | **A.** Thread `random_seed=seed` into the split call in both affected scripts, and rebuild features/`region_inputs` fresh per seed (not once outside the loop) — matching how SPIRE's own scripts already work.<br><br>**B.** Note in the Methodology that all "5-run" averages (baselines and GNNs alike) now re-randomize the train/val split per run, so std reflects both data-split and model-init variance consistently across the board. | ✓ | **Fixed and verified.** `run_baseline.py`: moved `_build_region_inputs()` inside `_run_one_seed()` (was built once before the seed loop), threaded `seed` into `create_spatial_split_indices(..., random_seed=seed)`. `run_exp4_proportion.py`: added `random_seed=seed` to the `create_spatial_split_indices` call in `_run_region()` (this affects SPIRE/GCN/GAT's own split for Exp4 too, not just baselines, since that function builds both). Fixed together with the (12)-A addendum (same functions touched) — see that row for the shared verification (`py_compile`, imports, real end-to-end runs on Middle East for all 6 files). Direct confirmation: `_build_region_inputs(..., seed=42)` vs `seed=50)` now produce genuinely different `xgb_score` columns (previously identical regardless of seed). | `experiment/run_baseline.py`<br>`experiment/run_exp4_proportion.py`<br>`data/Global_REE_occurrence_database_processed.xlsx` | No dedicated result file — bug fix verified via (12)-A's shared smoke tests; takes effect in `result/baseline.json`/`result/exp4_proportion.json` once those are rerun |

## 1. Problems requiring a choice between solutions

- **(6)**: run the optional C (MLP-no-graph control) or stop at A+B.
- **(10)**: choose Recast or Extend — the root decision, which also affects (2)-E and (8).
- ~~**(14)**: use the unified edge rule (A, recommended) or keep the asymmetric rule and justify it empirically (B).~~ **Decided: A (unified rule)**, wired as the new default across all 9 experiment scripts — see (14)'s Edited column.

## 2. Order for fixing the problems

- **Group 0 — decide first:** (10), since it determines the scope needed for (2)/(8).
- **Group 1 — fix at the source, in dependency order:** (11) → (12) → (13) → (14) → rerun all 6 main experiments.
- **Group 2 — independent, no dependency on Group 0 or 1, can start immediately in parallel with everything:** (3), (5).
- **Group 3 — needs the pipeline fixed in Group 1, in this order:** (1)-A/B → (17)-C, (18)-A → (2)-C → (17)-A → (6)-C/(17)-B → (6)-A → (17)-D → (7) → (18)-B → (4) → (2)-A → (2)-B → (2)-D → (1)-C → (17)-E. *(8) dropped — moot now that (10) is Recast, see (8)'s own Edited note.)*
- **Group 4 — final integration:** (9).

## 3. Problems that only need a text fix

Comments where the fix is writing/editing the manuscript itself — no rerun, no new code, no new experiment.

- **(3) Related Work Coverage** — rewrite the "Graph neural networks in the geosciences" paragraph around the 5 sub-areas, with real citations and a positioning sentence for SPIRE. No analysis needed, just literature lookup + writing. Ready now.
- **(5) Undefined Region Boundaries** — add a supplementary table (Region → countries + n, taken directly from the existing `Region` field), extend the existing `fig:world_overview` with a country outline/legend, and add 1-2 clarifying sentences on how graph edges cross region boundaries. Uses data/figures that already exist. Ready now.
- **(16) Unseen Region vs. Unseen Node** — fix the mislabeled `fig:res_A1` caption and audit the manuscript so "cross-region"/"unseen-region generalization" is used only for Exp1/Exp4, with Exp3/Exp6 relabeled "global inductive prediction of held-out nodes." Pure terminology/caption cleanup. Ready now.
- **(9) Results Section Lacks Geological Interpretation** — the fix is text-only (integrate 2-3 sentences of geological interpretation after each table/figure), but it draws on the results of (1), (7), (8), so it can't actually be written until those land. Text-only in nature, but blocked until Group 3 finishes.

Not included: comments like (2), (4), (6), (17), (18) do contain one or two text-only sub-items (e.g. reframing a claim, auditing the Conclusion), but the comment as a whole still requires reruns/new analysis, so they stay out of this list.
